PROJECT(TLibC)

FILE(GLOB_RECURSE HFILE_LIST ${INCLUDE}/*.h)
FILE(GLOB_RECURSE CFILE_LIST ${SOURCE}/*.c)


INCLUDE_DIRECTORIES(
	${INCLUDE}
	)

ADD_LIBRARY(TLibC ${HFILE_LIST} ${CFILE_LIST})
SET_TARGET_PROPERTIES(TLibC PROPERTIES OUTPUT_NAME tlibc)
INSTALL(TARGETS TLibC ARCHIVE DESTINATION ${LIBRARY_PATH})
